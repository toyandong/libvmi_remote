client.py在集中监控服务器上, server.py在dom0上
字符串作为函数名？

2013-12-07
工作任务：
  拷贝文件
  localhost运行libvmi
  写rpc版本libvmi：
  	集中监控多机
  	不同的机子，不同的策略
  	一个困难的地方---RPC中libvmi会话的保存。
  	如何唯一识别一个libvmi会话，ip+id。
  	存储的方式呢？在dom0端建立一个数组。server端也建立一个数组
  改写语义解析模块，能给出一些语义信息，部署到bc项目中
  然后不断改进。。。
  
  运行一个xp虚拟机
  运行ubuntu9虚拟机,ubuntu10虚拟机
  
  解决socket传输的时候，数据的类型问题
  
  TODO：
  解决多机监控的问题
  建立两个平台，进行实验对比
  
  
  
  
  
  
  

